#include <iostream>
using namespace std;

int main(){
    
    char ch;
    cin>>ch;
    if(ch=='a' || ch=='A' || ch=='e' || ch=='E' || ch=='i' || ch=='I' || ch=='o' || ch=='O' || ch=='u' || ch=='U') cout<<("Vowel")<<'\n';
    else cout<<("Not Vowel")<<'\n';
    return 0;
}

//Contributed by Raghav Garg