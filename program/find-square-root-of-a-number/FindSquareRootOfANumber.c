#include <stdio.h>
#include <math.h>

int main()
{

    int input;
    int output;
    printf("Enter the input number \n");
    scanf("%d", &input);
    output = sqrt(input);
    printf("%d", output);
}