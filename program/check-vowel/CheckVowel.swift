import Swift
import Foundation

var given = "a"

if ((given == "a") 
    || (given == "e") 
    || (given == "i") 
    || (given == "o") 
    || (given == "u") 
    || (given == "A") 
    || (given == "E") 
    || (given == "I") 
    || (given == "O") 
    || (given == "U")) {
  print("\(given) is a vowel")
} else {
  print("\(given) is not a vowel")
}
