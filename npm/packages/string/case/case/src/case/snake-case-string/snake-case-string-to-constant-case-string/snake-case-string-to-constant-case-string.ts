/**
 * Converts a snake case string to constant case string.
 *
 * @param str - The snake case string to convert.
 * @returns The constant case string.
 */
export function SnakeCaseStringToConstantCaseString(str: string): string {
  return str.toUpperCase();
}
